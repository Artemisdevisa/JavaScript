const cliente = 'juan'

// El tipo de variable let se puede declarar y luego modificar en valor, mientras que const unicamente se debe
// declarar e asignar valor en el momento en que se crea
// not defined -> la variable no ha sido creada aun
// undefined -> La variable no se la a ha asignado algún valor

console.log(cliente)